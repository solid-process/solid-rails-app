<%=
  resource_name, path =
    controller_name == "lists" ? ["list", new_web_task_list_path] : ["item", new_web_task_item_path(back_to:)]

  link_to(" + New #{resource_name}", path, class: is_current_page["new"] )
%>
